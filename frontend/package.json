{
  "name": "repo-risk-analyzer-frontend",
  "version": "0.0.1",
  "description": "Placeholder Angular frontend (static placeholder). Use backend API directly or replace with a full Angular app.",
  "scripts": {
    "start": "echo \"Frontend is a placeholder. Use backend API directly.\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
