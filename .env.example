# Environment example for Repo Risk Analyzer backend
# Path to local repository to analyze (optional). You can pass other paths via API.
REPO_PATH=

# Optional: LLM provider api key if integrating later
LLM_API_KEY=
LLM_API_URL=
